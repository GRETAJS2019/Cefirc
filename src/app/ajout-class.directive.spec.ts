import { AjoutClassDirective } from './ajout-class.directive';

describe('AjoutClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AjoutClassDirective();
    expect(directive).toBeTruthy();
  });
});
